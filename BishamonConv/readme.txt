Bishamon コンバータ

■ライセンスファイルについて
コンバータを使用するにはライセンスファイルが必要なので別途用意して入れること。
ライセンスファイルは慎重に取り扱うためバージョン管理の管理下には置かない。

■database 以下のフォルダ構成について
コンバートするには以下の３つのフォルダ名が合ってないとコンバートされない
※BISHAMON のデフォルトフォルダ名なので問題ないはず

database\bmsln
database\model
database\texture

